namespace BasicGameFrameworkLibrary.Core.BasicGameDataClasses;
public enum EnumSuggestedOrientation //not sure if this is needed.   will leave for now but could take out for next version.
{
    //for now still leave so there is option for maui to do something with it (blazor wpf can't do anything)
    Landscape, Portrait
}