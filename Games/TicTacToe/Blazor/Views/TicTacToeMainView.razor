@inherits BasicGameView<TicTacToeMainViewModel>
@using cc1 = CommonBasicLibraries.BasicDataSettingsAndProcesses.SColorString;
@{
    GetWin();
}
@if (_save == null)
{
    return;
}
<GridGameBoard SpaceList="_save.GameBoard" S="SpaceInfoCP"
               Context="Item" GridWidth="100%" GridHeight="80vh"
               TargetSpaceHeight="300" TargetSpaceWidth="300">
    <ItemTemplate Context="Space">
        <BorderedCommandSpace CommandObject="DataContext!.MakeMoveCommand"
                              CommandParameter="Space"
                              FillColor="@cc1.White"
                              BorderWidth="5"
                              BorderColor="@cc1.Black">
            <CenteredTextBlazor TextColor="@cc1.White"
                                BorderWidth="5"
                                FontSize="300"
                                Text="@GetText(Space)"
                                BorderColor="@cc1.Blue" />
        </BorderedCommandSpace>
    </ItemTemplate>
    <Canvas>
        @if (_win != null && _win.WinList.Count == 3)
        {
            <LineComponent Win="_win" />
        }
    </Canvas>
</GridGameBoard>
<LabelGridComponent DataContext="DataContext!.VMData" Labels="_labels" />