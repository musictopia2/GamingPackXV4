<BaseFrameBlazor IsEnabled="@IsEnabled" Text="@Player!.NickName">
    @{
        RegularSimpleCard? card = LastCard;
        @if (card is null)
        {
            return;
        }
    }
    @ChildContent!(card)
</BaseFrameBlazor>