@if (SorryDicedGameGameContainer.CanStart == false)
{
    return;
}
<BaseFrameBlazor Text="Start" >
    <GridTwoByTwo ColumnLength="@Columns">
        @{
            int row = 1;
            int column = 1;
        }
        @foreach (var player in Players)
        {
            <GridItem Column="column" Row="row">
                <PawnComponent HowMany="4" ImageHeight="@ImageHeight" ColorUsed="player.Color" />
            </GridItem>
            column++;
            if (column > 2)
            {
                column = 1;
                row = 2;
            }
        }
    </GridTwoByTwo>
    
</BaseFrameBlazor>
